"""Cross-cutting platform tooling (non-service packages).

Currently hosts ``tools.observability`` (CMP-DEPLOY-03), the OpenTelemetry
initialisation + structured-logging surface every service imports at boot.
"""

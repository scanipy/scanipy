extracted_by: pipeline
tool_versions:
  node: "24.13.0"
  typescript_eslint: "6.18.0"
  jelly: null
  tsc: null
known_limitations: |
  v0.1.0 call-graph + PDG come from pipeline/extract_ground_truth.mjs, a
  documented intraprocedural resolver (hand-verifiable on these small programs),
  NOT yet Jelly 1.4 / tsc (CLAR-CORP-07). Higher-order indirection (a returned
  function bound to a local, then called) and TS type-informed dispatch are
  conservatively tagged `dynamic` and EXCLUDED from gate precision/recall.
  Sites at eval / new Function / computed member access (obj[name]()) are
  likewise tagged `dynamic`. AST extraction IS the pinned production tool
  (@typescript-eslint/typescript-estree 6.18.0).
review_status: pipeline

{
  "schema": "cpg-fidelity/pdg/v1",
  "program_id": "0004-lambdas",
  "method": "Intraprocedural PDG (data) by-inspection.",
  "methods": [
    {"method": "corpus.cpg.p0004.Lambdas.apply(Function,int)", "dependences": [
      {"from": "param f", "to": "return f.apply(x)", "type": "data", "var": "f"},
      {"from": "param x", "to": "return f.apply(x)", "type": "data", "var": "x"}
    ]},
    {"method": "corpus.cpg.p0004.Lambdas.main(String[])", "dependences": [
      {"from": "Function inc = x -> x + 1", "to": "int a = apply(inc, 10)", "type": "data", "var": "inc"},
      {"from": "int a = apply(inc, 10)", "to": "System.out.println(a + b)", "type": "data", "var": "a"},
      {"from": "int b = apply(n -> twice(n), 5)", "to": "System.out.println(a + b)", "type": "data", "var": "b"}
    ]}
  ]
}
